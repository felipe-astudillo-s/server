# Credenciales del hoster: NUNCA al repo.
.auth_tokens.json

# Configuracion local del hoster (se genera sola)
backup.properties

# Zips temporales y logs
backup-*.zip
*.log

# Carpetas de mundo, por si el repo se descomprime dentro de un server
world/
world_nether/
world_the_end/

# Salida de compilacion
build/
*.jar
*.class
